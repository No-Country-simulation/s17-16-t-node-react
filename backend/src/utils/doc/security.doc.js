
/**
 * @swagger
 * components:
 *  securitySchemes:
 *    bearerAuth:
 *      type: http
 *      scheme: bearer
 *      bearerFormat: JWT
 *    apiKeyAuth:
 *      type: apiKey
 *      scheme: apiKey
 *      name: apiKey
 *      in: header
 *    cookieAuth:
 *      type: apiKey
 *      scheme: apiKey
 *      name: apiKey
 *      in: cookieconsole.log(((60 - (res.get("RateLimit-Reset") % 60)) / 60).toFixed(2));
 */

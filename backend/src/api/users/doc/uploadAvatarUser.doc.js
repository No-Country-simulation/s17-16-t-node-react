/**
 * @swagger
 * components:
 *   schemas:
 *     uploadAvatar:
 *       name: Upload image avatar.
 *       description: Upload image avatar.
 *       type: object
 *       properties:
 *         avatar:
 *           type: string
 *           format: binary
 *         name:
 *           type: string
 *         lastName:
 *          type: string
 *         dni:
 *           type: string
 *         email:
 *           type: string
 *         password:
 *           type: string
 *         phone:
 *           type: string
 *         role:
 *           type: string
 */

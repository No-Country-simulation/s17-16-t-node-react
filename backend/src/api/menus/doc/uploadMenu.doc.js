/**
 * @swagger
 * components:
 *   schemas:
 *     uploadPicture:
 *       name: Upload image file.
 *       description: Upload image file.
 *       type: object
 *       properties:
 *         picture:
 *           type: string
 *           format: binary
 *         name:
 *           type: string
 *         description:
 *           type: string
 *         price:
 *           type: string
 *         category:
 *           type: string
 *         restaurant:
 *           type: string
 */
